# WindsurfRules — Alpine Go Performance Starter

The canonical agent context for this repo lives in [`AGENTS.md`](AGENTS.md) at the
repo root — read it. It is generated from `CLAUDE.md` and the `.claude/*.md` layers
via `task agents:build`; do not duplicate those rules here.

## Cascade-specific directive

- When you apply a project rule, explicitly state which rule (and its ADR) you are
  following in your output, so the operator can audit the decision.
