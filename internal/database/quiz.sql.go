// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: quiz.sql

package database

import (
	"context"

	"github.com/google/uuid"
)

const countCorrectAttemptsByUser = `-- name: CountCorrectAttemptsByUser :one
SELECT COUNT(*)
FROM quiz_attempts
WHERE user_id = $1 AND is_correct = true
`

func (q *Queries) CountCorrectAttemptsByUser(ctx context.Context, userID uuid.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countCorrectAttemptsByUser, userID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createQuizAttempt = `-- name: CreateQuizAttempt :one
INSERT INTO quiz_attempts (
    user_id, question_id, selected_index, is_correct
) VALUES (
    $1, $2, $3, $4
)
RETURNING id, user_id, question_id, selected_index, is_correct, created_at
`

type CreateQuizAttemptParams struct {
	UserID        uuid.UUID `json:"user_id"`
	QuestionID    uuid.UUID `json:"question_id"`
	SelectedIndex int32     `json:"selected_index"`
	IsCorrect     bool      `json:"is_correct"`
}

func (q *Queries) CreateQuizAttempt(ctx context.Context, arg CreateQuizAttemptParams) (QuizAttempt, error) {
	row := q.db.QueryRow(ctx, createQuizAttempt,
		arg.UserID,
		arg.QuestionID,
		arg.SelectedIndex,
		arg.IsCorrect,
	)
	var i QuizAttempt
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.QuestionID,
		&i.SelectedIndex,
		&i.IsCorrect,
		&i.CreatedAt,
	)
	return i, err
}

const getQuizQuestion = `-- name: GetQuizQuestion :one
SELECT id, slug, topic, prompt, choices, correct_index, explanation, created_at
FROM quiz_questions
WHERE id = $1 LIMIT 1
`

func (q *Queries) GetQuizQuestion(ctx context.Context, id uuid.UUID) (QuizQuestion, error) {
	row := q.db.QueryRow(ctx, getQuizQuestion, id)
	var i QuizQuestion
	err := row.Scan(
		&i.ID,
		&i.Slug,
		&i.Topic,
		&i.Prompt,
		&i.Choices,
		&i.CorrectIndex,
		&i.Explanation,
		&i.CreatedAt,
	)
	return i, err
}

const getQuizQuestionBySlug = `-- name: GetQuizQuestionBySlug :one
SELECT id, slug, topic, prompt, choices, correct_index, explanation, created_at
FROM quiz_questions
WHERE slug = $1 LIMIT 1
`

func (q *Queries) GetQuizQuestionBySlug(ctx context.Context, slug string) (QuizQuestion, error) {
	row := q.db.QueryRow(ctx, getQuizQuestionBySlug, slug)
	var i QuizQuestion
	err := row.Scan(
		&i.ID,
		&i.Slug,
		&i.Topic,
		&i.Prompt,
		&i.Choices,
		&i.CorrectIndex,
		&i.Explanation,
		&i.CreatedAt,
	)
	return i, err
}

const listQuizAttemptsByUser = `-- name: ListQuizAttemptsByUser :many
SELECT id, user_id, question_id, selected_index, is_correct, created_at
FROM quiz_attempts
WHERE user_id = $1
ORDER BY created_at DESC
LIMIT $2 OFFSET $3
`

type ListQuizAttemptsByUserParams struct {
	UserID uuid.UUID `json:"user_id"`
	Limit  int32     `json:"limit"`
	Offset int32     `json:"offset"`
}

func (q *Queries) ListQuizAttemptsByUser(ctx context.Context, arg ListQuizAttemptsByUserParams) ([]QuizAttempt, error) {
	rows, err := q.db.Query(ctx, listQuizAttemptsByUser, arg.UserID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []QuizAttempt{}
	for rows.Next() {
		var i QuizAttempt
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.QuestionID,
			&i.SelectedIndex,
			&i.IsCorrect,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listQuizQuestions = `-- name: ListQuizQuestions :many
SELECT id, slug, topic, prompt, choices, correct_index, explanation, created_at
FROM quiz_questions
ORDER BY created_at ASC
`

func (q *Queries) ListQuizQuestions(ctx context.Context) ([]QuizQuestion, error) {
	rows, err := q.db.Query(ctx, listQuizQuestions)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []QuizQuestion{}
	for rows.Next() {
		var i QuizQuestion
		if err := rows.Scan(
			&i.ID,
			&i.Slug,
			&i.Topic,
			&i.Prompt,
			&i.Choices,
			&i.CorrectIndex,
			&i.Explanation,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
